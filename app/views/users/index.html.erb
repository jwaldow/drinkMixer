<h1>Listing users</h1>
<!-- START_HIGHLIGHT -->
  <% if notice %>
    <p id="notice"><%= notice %></p>
  <% end %>
<!-- END_HIGHLIGHT -->

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <!--<td><%= user.username %></td>-->
    <td><%= image_tag user.pic.url, size: "100x100" %></td>
    <td><%= link_to user.username, user %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>