<%= form_for(@cabinet_item) do |f| %>
  <% if @cabinet_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cabinet_item.errors.count, "error") %> prohibited this cabinet_item from being saved:</h2>

      <ul>
      <% @cabinet_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ingredient_id %><br />
    <%= f.number_field :ingredient_id %>
  </div>
  <div class="field">
    <%= f.label :cabinet_id %><br />
    <%= f.number_field :cabinet_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
