<!DOCTYPE html>
<html>
<head>
  <title>DrinkMixer</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="banner">
		<!-- this is the place where I put the header info -->
		<h1>Make Me a Drink!</h2>
	</div>

	<div id="side_bar">
	<% if session[:user_id] %>
		<% @user = User.find_by_id(session[:user_id]) %>
		<h2>Greeting today <%= @user.username %></h2>
		<br />
		<%= link_to 'Users' , users_path %><br />
		<br />
		<%= button_to 'Logout' , logout_url, :method => :delete %>
	<% else %>
		<br />
		<%= button_to 'Log In' , login_url %><br />
		<br />
		<%= button_to 'Register' , new_user_path, :method => :get %><br />
	<% end %>
	</div>

	<div id="content">
		<% if notice %>
    		<p id="notice"><%= notice %></p>
  		<% end %>
		<%= yield %>
	</div>
</body>
</html>
